# @pattern 替换模式指南

## 概述

`@pattern` 是一个灵活的替换模式注释，用于定义别名替换时的范围。通过 `%` 占位符表示选中的值，可以实现各种框架和语法的智能替换。

## 基本语法

**推荐格式（一行）：**
```css
// @alias <别名> <模式>
--token-name: <值>;
```

**兼容格式（两行）：**
```css
// @alias <别名>
// @pattern <模式>
--token-name: <值>;
```

- `%` 代表选中的值
- 模式定义了替换时需要包含的前后文本
- 模式是可选的，不写模式则只替换值本身

## 使用场景

### 1. Tailwind CSS 方括号语法

**定义：**
```css
// @alias xl [%]
--spacing-xl: 20px;
```

**使用：**
```html
<!-- 原始代码 -->
<div class="text-[20px]">

<!-- 选中 20px，点击别名图标 -->
<div class="text-xl">
```

**解释：**
- `[%]` 表示：如果选中的值在 `[...]` 中
- 替换时会把整个 `[20px]` 替换为 `xl`

### 2. CSS 变量函数

**定义：**
```css
/* @alias primary var(%%) */
--color-primary: #1E90FF;
```

**使用：**
```css
/* 原始代码 */
.button {
  color: var(#1E90FF);
}

/* 选中 #1E90FF，点击别名图标 */
.button {
  color: primary;
}
```

**解释：**
- `var(%%)` 表示：如果选中的值在 `var(...)` 中
- 替换时会把整个 `var(#1E90FF)` 替换为 `primary`

### 3. 函数调用语法

**定义：**
```css
// @alias ease cubic-bezier(%)
--timing-ease: 0.25, 0.1, 0.25, 1;
```

**使用：**
```css
/* 原始代码 */
.transition {
  transition-timing-function: cubic-bezier(0.25, 0.1, 0.25, 1);
}

/* 选中值，点击别名图标 */
.transition {
  transition-timing-function: ease;
}
```

### 4. 无模式（只替换值本身）

**定义：**
```css
// @alias accent
--color-accent: #FF6B6B;
```

**使用：**
```css
/* 原始代码 */
.text {
  color: #FF6B6B;
}

/* 选中 #FF6B6B，点击别名图标 */
.text {
  color: accent;
}
```

**解释：**
- 没有 `@pattern` 时，只替换选中的值本身
- 不会扩展替换范围

## 模式示例

### 常用模式

| 框架/场景 | 定义示例 | 使用效果 |
|----------|---------|---------|
| Tailwind CSS | `// @alias xl [%]` | `text-[20px]` → `text-xl` |
| CSS 变量 | `// @alias primary var(%%)` | `var(#1E90FF)` → `primary` |
| calc() | `// @alias full calc(%)` | `calc(100%)` → `full` |
| RGB 函数 | `// @alias primary rgb(%)` | `rgb(30,144,255)` → `primary` |
| 括号包裹 | `// @alias xl (%)` | `(20px)` → `xl` |
| 引号包裹 | `// @alias xl "%"` | `"20px"` → `"xl"` |

### 复杂模式

**嵌套函数：**
```css
// @alias shadow-sm drop-shadow(%)
--shadow-small: 0 1px 2px rgba(0,0,0,0.1);
```

**多层包裹：**
```css
// @alias safe-area calc(env(%))
--safe-area-top: constant(safe-area-inset-top);
```

## 工作原理

1. **解析阶段**：从 CSS 注释中提取 `@pattern`
2. **匹配阶段**：当选中值时，检查周围文本是否匹配模式
3. **替换阶段**：
   - 有 `@pattern` 且匹配：替换整个模式部分
   - 有 `@pattern` 但不匹配：只替换选中值
   - 无 `@pattern`：只替换选中值

### 示例流程

```
代码：text-[20px]
      ^^^^^^
      选中位置

模式：[%]
分析：
- 前面：[
- 选中：20px
- 后面：]

匹配：✅ 符合 [%] 模式

替换范围：[20px]
替换结果：xl
最终：text-xl
```

## 注意事项

### ✅ 推荐做法

1. **明确使用场景**
   ```css
   // Tailwind 专用
   // @alias xl [%]
   --spacing-xl: 20px;
   ```

2. **一行定义，简洁明了**
   ```css
   // ✅ 推荐：一行完成
   // @alias xl [%]
   
   // ⚠️ 可以但不推荐：分两行
   // @alias xl
   // @pattern [%]
   ```

3. **模式要具体**
   ```css
   // ✅ 好：明确的方括号
   // @alias xl [%]
   
   // ❌ 避免：过于宽泛
   // @alias xl %
   ```

### ❌ 常见错误

1. **忘记 % 占位符**
   ```css
   // ❌ 错误：没有 %
   // @alias xl []
   
   // ✅ 正确
   // @alias xl [%]
   ```

2. **模式不匹配实际使用**
   ```css
   // @alias primary [%]
   --color: #1E90FF;
   
   /* 实际使用 */
   color: #1E90FF;  /* 不在方括号中，模式不匹配，只替换值 */
   ```

3. **别名中包含空格**
   ```css
   // ❌ 错误：别名有空格
   // @alias text xl [%]
   
   // ✅ 正确：使用连字符
   // @alias text-xl [%]
   ```

## 高级技巧

### 1. 同一值，不同场景

```css
:root {
  /* Tailwind 场景 */
  // @alias xl [%]
  --spacing-xl: 20px;
  
  /* 普通 CSS 场景 */
  // @alias spacing-large
  --spacing-large: 20px;
}
```

使用时：
- `text-[20px]` → 选择 `--spacing-xl` → 得到 `text-xl`
- `padding: 20px` → 选择 `--spacing-large` → 得到 `padding: spacing-large`

### 2. 条件替换

只有当值在特定上下文中才使用别名：

```css
// @alias primary [%]
--color-primary: #1E90FF;
```

- `bg-[#1E90FF]` → 匹配模式 → `bg-primary`
- `color: #1E90FF` → 不匹配模式 → 只替换值为 `primary`

### 3. 组合使用

```css
// Tailwind 工具类别名（有模式）
// @alias xl [%]
--size-xl: 20px;

// 点击别名按钮：根据上下文智能替换
// 回车键：始终替换为 var(--size-xl)
```

## 测试建议

使用 `test-alias-example.css` 中的示例进行测试：

1. 打开 `test-usage-demo.html`
2. 选中不同场景下的值
3. 观察模式匹配和替换效果
4. 验证边界情况

## 总结

- ✅ `@pattern` 让别名替换更智能、更灵活
- ✅ 不再写死特定框架的逻辑
- ✅ 每个 token 可以有自己的替换规则
- ✅ 支持各种语法：方括号、函数、引号等
- ✅ 可选功能：不影响现有的别名替换

**灵活性 + 通用性 = 更好的开发体验！** 🎉

